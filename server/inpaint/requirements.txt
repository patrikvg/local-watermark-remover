# Install CUDA torch first (see README), then:
#   pip install --no-deps simple-lama-inpainting==0.1.2
#   pip install -r requirements.txt
# Do not list simple-lama-inpainting here: its metadata pins pillow<10 / numpy<2,
# which fail to build on Python 3.13.
fire==0.5.0
opencv-python-headless==4.10.0.84
